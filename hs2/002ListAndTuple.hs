-- List 需要元素为 同一种类型

-- List 是在空列表 [] 的基础上 通过 x: [xs] 构建的
-- [1, 2, 3, 4, 5] 可以理解为 1:2:3:4:5:[]

-- 元组，一种多值记法
-- 长度为 n 的元组， 称为 n-tuple, 如果长度为2，称为pairs, 长度为3称为 triples

-- fst snd 函数可以取 元组 第一个和第二个值, 不过只能用在长度为2的元组上

